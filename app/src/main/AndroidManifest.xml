<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Network access for the Claude API / CLI -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Notifications for long-running agent/terminal tasks (foreground service) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      Storage: the app works inside its private sandbox; access to user folders
      is granted at runtime via the Storage Access Framework (SAF) instead of
      broad storage permissions. MANAGE_EXTERNAL_STORAGE was removed because it
      is rejected by Google Play and not needed for the MVP.
    -->

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher_round"
        android:label="Claude Terminal"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClaudeMobile"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
